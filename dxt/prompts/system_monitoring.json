{
  "name": "system_status_check",
  "description": "Monitor VRChat MCP server system status and health",
  "version": "1.0.0",
  "parameters": {
    "check_type": {
      "type": "string",
      "enum": ["health", "status", "performance", "osc_stats"],
      "description": "Type of system check to perform",
      "default": "status"
    },
    "detailed": {
      "type": "boolean",
      "description": "Whether to include detailed information",
      "default": false
    }
  },
  "template": "Check VRChat MCP server {check_type}. {detail_instruction} Monitor system health, OSC connections, and performance metrics.",
  "examples": [
    {
      "input": {
        "check_type": "health",
        "detailed": true
      },
      "output": "Check VRChat MCP server health with detailed information. Monitor system health, OSC connections, and performance metrics."
    },
    {
      "input": {
        "check_type": "osc_stats"
      },
      "output": "Check VRChat MCP server osc_stats. Monitor system health, OSC connections, and performance metrics."
    }
  ]
}
